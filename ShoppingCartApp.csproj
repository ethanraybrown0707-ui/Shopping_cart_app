<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <PropertyGroup>
    <!-- Attribution only - this is a text field embedded in the binary (visible under the
         .exe's Properties > Details tab), not a cryptographic signature. It does not affect
         Smart App Control or SmartScreen; see sign-exe.ps1 for the actual code-signing step,
         which also doesn't satisfy Smart App Control (see that script's comments for why). -->
    <Company>Ethan Brown</Company>
    <Authors>Ethan Brown</Authors>
    <Product>ShoppingCartApp</Product>
  </PropertyGroup>

</Project>
